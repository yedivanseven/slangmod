[model]
dim = 256
n_heads = 4
n_layers = 6

[data]
seq_len = 512
stride = 384
jitter = 32

[tokens]
vocab = 16384

[train]
batch_size = 128
learning_rate = 0.01  # ToDO: Try if 0.15 sticks
warmup = 4_000  # ToDo: Scale warmup with batch size on top of model size increase
cooldown = 23_000  # ToDo: Try 25_000 with 0.005
